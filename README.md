# Agent of Code 2025

Run an agent to solve Advent of Code. Generate an OpenAI API Key and copy it
into the module attribute `@api_key` in `agent_of_code.exs`.

Run the agent with `elixir agent_of_code.exs`.
